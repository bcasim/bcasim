# Disconnect both directions at 200, reconnect at 600; public history is synchronized.
seed=42
simulation.time=1000
consensus=PoW
block.interval=10
nodes.weights=0.4,0.6
nodes.strategies=honest,honest
network.matrix=0,1;1,0
network.blockDelay=1
network.transactionDelay=0.1
network.changes=200:disconnect:0:1;200:disconnect:1:0;600:connect:0:1;600:connect:1:0
transaction.generate=true
transaction.rate=0.5
transaction.value=1
transaction.initialBalance=1000
observer.node=1
